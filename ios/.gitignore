# XcodeGen generates the project from project.yml — don't track it.
Prefrontal.xcodeproj/
# XcodeGen also generates each target's Info.plist.
PrefrontalWidgets/Info.plist
Prefrontal/Info.plist
PrefrontalWatch/Info.plist
PrefrontalWatchWidgets/Info.plist
# Local signing override (your Team ID) — never committed; see Signing.xcconfig.
Signing.local.xcconfig
# Build output
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store
